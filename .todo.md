# TOOO:
- Automatic deployment to PyPI with GitHub Actions.
- Automatic upysm generation with GitHub Actions.
- upysm tests with GitHub Actions.
- port extra features to upysm.
- FUNDING file.
- Automatic docs generation with GitHub Actions.
- Minimize pysm version to have the smallest possible memory footprint.
- Automatic release to pypi with GitHub Actions? Do we want that?
- How do I create a new release? Do I just push a tag? Do I need to update the version in the code? Do I need to update the changelog?
- automated tagging on version bumping.
<!-- - Remove Travis CI and replace it with GitHub Actions. -->
<!-- - Update documentation to reflect new features and changes. -->
<!-- - Change README.rst to be a mix of the new version and the old version. -->


I'm thinking that the version bumping is too naive. Perhaps we should rely on the automatic bump ups only if changes in the ./pysm directory are made. Otherwise version should be updated maunually. What do you think?